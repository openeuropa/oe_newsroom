# OpenEuropa Newsroom VCR

_Do not enable in production._

This submodule intercepts outgoing http requests, either to record real requests and responses, or to replay pre-recorded responses and assert pre-recorded requests.allows to make requests to the connected Newsroom API, by calling methods in the client.

The module is mostly designed for tests, but can be installed in a local development environment.
